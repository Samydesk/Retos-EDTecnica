let valores = [1000, -50, -100, 1, 0, 21, 3, -1000, 99];

function indicesValue(arreglo) {
  for (let i = 0; i < arreglo.length; i++) {
    console.log(`El valor en el índice ${i} es: ${arreglo[i]}`);
  }
}

let = indicesValue(valores);